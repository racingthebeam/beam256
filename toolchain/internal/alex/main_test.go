package alex

import (
	"testing"
	// "github.com/stretchr/testify/assert"
)

func TestLexer(t *testing.T) {
	// d := Define([]Rule{
	// 	R("[a-z]+", 1),
	// 	R(`\s+`, 2),
	// }, nil)

	// l := d.New("  foo   bar   baz     ")

	// tok, text, _ := l.Next()
	// assert.Equal(t, 2, tok)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 1, tok)
	// assert.Equal(t, "foo", text)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 2, tok)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 1, tok)
	// assert.Equal(t, "bar", text)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 2, tok)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 1, tok)
	// assert.Equal(t, "baz", text)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, 2, tok)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, EOF, tok)
	//
	// tok, text, _ = l.Next()
	// assert.Equal(t, EOF, tok)
}
